function main()
    
    %Read text file with incomming encoded message
    encoded_message_FILE = fopen('../TX/mensaje_codificado.txt','r'); 
    encoded_message = [];
    
    while ~feof(fid) % feof(fid) is true when the file ends
        encoded_message = [encoded_message; fgetl(encoded_message_FILE)]; % read one line
    end
    
    fclose(encoded_message_FILE);
end